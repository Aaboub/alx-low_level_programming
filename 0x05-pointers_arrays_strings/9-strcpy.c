#include <string.h>
#include <stdio.h>
#include "main.h"

/**
 * _strcpy - Entry
 *
 * @dest: param
 * @src: param
 *
 * Return: pointer to char
 */
char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
